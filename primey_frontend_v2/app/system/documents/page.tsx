"use client";
import * as React from "react";
import {Activity,Database,FileSpreadsheet,Inbox,Printer,RefreshCw,RotateCcw,ShieldCheck} from "lucide-react";
import {toast} from "sonner";
import {Button} from "@/components/ui/button";
import {Card,CardContent,CardDescription,CardHeader,CardTitle} from "@/components/ui/card";
import {DataRegisterEmptyState,DataRegisterSearch,DataRegisterToolbar,registerBrandButtonClass,registerOutlineButtonClass} from "@/components/ui/data-register";
import {DataRegisterResultCount,DataRegisterTableFrame} from "@/components/ui/data-register-table";
import {SystemMetricCard} from "@/components/ui/system-metric-card";
import {Table,TableBody,TableCell,TableHead,TableHeader,TableRow} from "@/components/ui/table";
type O=Record<string,unknown>; const o=(v:unknown):O=>v&&typeof v==="object"&&!Array.isArray(v)?v as O:{}; const a=(v:unknown)=>Array.isArray(v)?v:[]; const s=(v:unknown)=>v==null||v===""?"—":typeof v==="object"?JSON.stringify(v):String(v);
export default function Page(){const[p,setP]=React.useState<O>({}),[q,setQ]=React.useState(""),[loading,setLoading]=React.useState(true);const apiBase=()=>{const raw=String(process.env.NEXT_PUBLIC_API_BASE_URL||process.env.NEXT_PUBLIC_API_URL||"").replace(/\/+$/,"");return raw.endsWith("/api")?raw.slice(0,-4):raw};const load=React.useCallback(async()=>{setLoading(true);try{const r=await fetch(`${apiBase()}/api/system/documents/`,{credentials:"include",headers:{Accept:"application/json"},cache:"no-store"});const ct=String(r.headers.get("content-type")||"").toLowerCase();if(!ct.includes("application/json")){const preview=(await r.text()).replace(/\s+/g," ").slice(0,120);throw new Error(`API returned ${r.status} ${ct||"non-JSON"}${preview?` — ${preview}`:""}`)}const j=await r.json();if(!r.ok||j.success===false)throw new Error(s(j.message));setP(j)}catch(e){toast.error(e instanceof Error?e.message:"Request failed")}finally{setLoading(false)}},[]);React.useEffect(()=>{void load()},[load]);const d=o(p.data),raw=a(p.results).length?a(p.results):a(d.checks).length?a(d.checks):a(d.latest_audit_events).length?a(d.latest_audit_events):a(d.companies).length?a(d.companies):a(d.latest_templates),rows=React.useMemo(()=>raw.map((x,i)=>{const r=o(x),c=o(r.company);return[String(i+1),s(r.label||r.event_type||r.name||c.name||c.display_name),s(r.key||r.source_app||c.company_code||r.app_label),s(r.status||r.severity),s(r.message||r.summary||r.description)]}),[raw]),shown=rows.filter(r=>r.join(" ").toLowerCase().includes(q.toLowerCase()));const sum=o(d.summary);return <main dir="rtl"><div className="space-y-6 p-4 lg:p-6"><header className="flex items-start justify-between gap-4"><div><h1 className="text-2xl font-bold">المستندات والطباعة</h1><p className="mt-1 text-sm text-muted-foreground">مركز تشغيلي للقراءة فقط من API النظام الحقيقي.</p></div><div className="flex gap-2"><Button variant="outline" className={registerOutlineButtonClass} onClick={()=>void load()}><RefreshCw/>تحديث</Button><Button className={registerBrandButtonClass} onClick={()=>window.print()}><Printer/>طباعة</Button></div></header><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><SystemMetricCard title="نماذج المستندات" value={Number(sum.document_models_count||0)} description="Document models" icon={Database}/><SystemMetricCard title="القوالب" value={Number(sum.template_records_count||0)} description="قوالب حقيقية" icon={ShieldCheck}/><SystemMetricCard title="مهام الطباعة" value={Number(sum.print_jobs_count||0)} description="Print jobs" icon={Activity}/><SystemMetricCard title="مسارات النظام" value={Number(sum.system_routes_available_count||0)} description="مسارات متاحة" icon={FileSpreadsheet}/></div><Card><CardHeader><CardTitle icon={Database}>السجل التشغيلي</CardTitle><CardDescription>نفس معيار V2 المعتمد في الشركات ولوحة النظام.</CardDescription></CardHeader><CardContent className="space-y-4"><DataRegisterToolbar><DataRegisterSearch value={q} onChange={setQ} placeholder="بحث..." className="flex-1"/><Button variant="outline" className={registerOutlineButtonClass} onClick={()=>setQ("")}><RotateCcw/>إعادة ضبط</Button></DataRegisterToolbar>{shown.length?<DataRegisterTableFrame><Table><TableHeader><TableRow><TableHead>ID</TableHead><TableHead>الاسم</TableHead><TableHead>السياق</TableHead><TableHead>الحالة</TableHead><TableHead>التفاصيل</TableHead></TableRow></TableHeader><TableBody>{shown.map((r,i)=><TableRow key={i} className="h-[68px]">{r.map((v,j)=><TableCell key={j}><span className={j===4?"line-clamp-2 text-xs text-muted-foreground":""}>{v}</span></TableCell>)}</TableRow>)}</TableBody></Table></DataRegisterTableFrame>:!loading?<DataRegisterEmptyState icon={Inbox} title="لا توجد بيانات" description="لا توجد سجلات حقيقية متاحة حاليًا."/>:null}<DataRegisterResultCount showingLabel="عرض" showingCount={shown.length} ofLabel="من" totalCount={rows.length} rowsLabel="سجل"/></CardContent></Card></div></main>}
